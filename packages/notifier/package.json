{
  "name": "@sparkkeeper/notifier",
  "version": "0.0.0",
  "private": true,
  "description": "SparkKeeper notification policy and webhook delivery package",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm --filter @sparkkeeper/shared build && tsx --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sparkkeeper/shared": "workspace:*"
  }
}
