{
  "name": "@sparkkeeper/admin-web",
  "version": "0.0.0",
  "private": true,
  "description": "SparkKeeper admin web foundation",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -p tsconfig.app.json --noEmit && vite build",
    "typecheck": "vue-tsc -p tsconfig.app.json --noEmit && tsc -p tsconfig.node.json --noEmit"
  },
  "dependencies": {
    "vue": "^3.5.20"
  }
}
